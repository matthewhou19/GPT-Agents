version: "3"
services:
  backend:
    build:
      context: ./backend/gpt-agents
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - mynetwork

  frontend:
    build:
      context: ./gptAgentsFE
      dockerfile: Dockerfile
    ports:
      - "80:80"
    networks:
      - mynetwork
    environment:
      - BACKEND_URL=http://backend:8080

networks:
  mynetwork:
